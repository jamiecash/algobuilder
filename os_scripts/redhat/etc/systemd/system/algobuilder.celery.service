[Unit]
Description=Celery worker service for AlgoBuilder
After=network.target

[Service]
Type=forking
EnvironmentFile=/etc/systemd/system/algobuilder.celery.conf
User=algobuilder
Group=algobuilder
WorkingDirectory=/opt/algobuilder
ExecStart=/bin/sh -c '${CELERY_BIN} multi start w1 -A $CELERY_APP --pidfile=${CELERYD_PID_FILE} --logfile=${CELERYD_LOG_FILE} --loglevel="${CELERYD_LOG_LEVEL}"'
ExecStop=/bin/sh -c '${CELERY_BIN} multi stopwait w1 --pidfile=${CELERYD_PID_FILE} --loglevel="${CELERYD_LOG_LEVEL}"'
ExecReload=/bin/sh -c '${CELERY_BIN} multi restart w1 -A $CELERY_APP --pidfile=${CELERYD_PID_FILE} --logfile=${CELERYD_LOG_FILE} --loglevel="${CELERYD_LOG_LEVEL}"'
Restart=always

[Install]
WantedBy=multi-user.target
